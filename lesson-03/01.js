// Напишите функцию calculateAverage, которая принимает 2 числа и возвращает их среднее арифметическое
const a = 6;
const b = 8;
function calculateAverage(num_a, num_b) {
    let c = (num_a + num_b) / 2;

    return c;
}

let result = calculateAverage(a, b);
console.log(result);